# ============================================
# SUBA&GO FRONTEND ENVIRONMENT CONFIGURATION
# ============================================

# Environment Configuration
# APP_ENV: local | development | production
# - local: Uses localhost:3000 and *.localhost:3000
# - development: Uses *.development.subago.cl
# - production: Uses *.subago.cl
#
# Note: We use APP_ENV instead of NODE_ENV because Next.js automatically
# overrides NODE_ENV to 'development' or 'production' based on the command.
APP_ENV=local
NEXT_PUBLIC_APP_ENV=local

# Backend API URL
BACKEND_URL=http://localhost:3001

# NextAuth Configuration
AUTH_SECRET="your-auth-secret-here"
AUTH_TRUST_HOST=true

# Domain Configuration
ROOT_DOMAIN=subago.cl
NEXT_PUBLIC_ROOT_DOMAIN=subago.cl

# Vercel Blob Storage (for file uploads)
BLOB_READ_WRITE_TOKEN="your-vercel-blob-token-here"

# ============================================
# ENVIRONMENT-SPECIFIC URLS
# ============================================
# Local:       http://*.localhost:3000
# Development: https://*.development.subago.cl
# Production:  https://*.subago.cl

